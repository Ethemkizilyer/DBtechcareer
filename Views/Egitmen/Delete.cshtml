@model Egitmen

<p>
    <b>@Model.Ad @Model.Soyad</b> isimli eğitmeni silmek istediğinizden emin misiniz?
    <form asp-action="Delete" method="post">
        <input type="hidden" value="@Model.EgitmenId" name="id">
        <button type="submit" class="btn btn-danger">Delete</button>
        <a asp-action="Index" class="btn btn-primary">Hayır</a>
    </form>
</p>